/**
 * Entites JPA (@Entity), mappees sur le schema PostgreSQL defini dans le MPD
 * (voir docs/ et database/schema.sql). Noms de colonnes fixes via @Column(name = "...").
 */
package com.golfe1.gpi.entities;
